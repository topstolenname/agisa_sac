# Observability Stack Environment Variables
# Copy this file to .env and set secure values

# Grafana Admin Credentials
# IMPORTANT: Set a strong password before deploying
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme_to_secure_password

# For production, use a strong randomly generated password:
# GRAFANA_ADMIN_PASSWORD=$(openssl rand -base64 32)
