# Prometheus configuration placeholder
global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'coordinator'
    static_configs:
      - targets: ['pcp-coordinator:8000']

  - job_name: 'agents'
    static_configs:
      - targets:
          - 'agent-1:9100'
          - 'agent-2:9100'

# Collected metrics: memory_usage, message_throughput, ethical_violation_count
# Future: create Grafana dashboards for ethical metrics
